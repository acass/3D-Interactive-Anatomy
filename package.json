{
  "name": "3d-interactive-explainer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "proxy": "tsx --env-file=.env server/proxy.ts",
    "proxy:test": "tsx --env-file=.env server/testclient.ts"
  },
  "dependencies": {
    "@google/genai": "^1.9.0",
    "@google/model-viewer": "^4.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
